{
  "name": "jaiwin-design-studio",
  "version": "1.0.0",
  "private": true,
  "description": "Static Jaiwin Design Studio website with Decap CMS and local live edit mode.",
  "scripts": {
    "preview": "node local-server.js",
    "cms": "decap-server",
    "check": "node --check main.js && node --check admin/preview.js && node --check editor.js && node --check local-server.js"
  },
  "devDependencies": {
    "decap-server": "^3.9.1",
    "express": "^4.19.2",
    "multer": "^2.2.0"
  }
}
